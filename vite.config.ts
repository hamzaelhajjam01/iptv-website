// Note: This project uses Next.js and does not rely on Vite.
// This file is kept as a no-op to avoid TypeScript import errors during Next build.
// If you migrate to Vite in the future, restore the previous configuration.

const noopConfig = {};
export default noopConfig;
