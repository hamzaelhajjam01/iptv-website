# Copy this file to `.env.local` and fill in your real credentials.
# Keep secrets out of source control. Do NOT commit your real API keys.

GEMINI_API_KEY=AIzaSyBcE1vZu6WSeJT-7pPOSyIkqHSBUwK_RDw

# Usage:
# 1. Copy this to .env.local:  copy .env.example .env.local
# 2. Replace the placeholder with your real Gemini API key.
# 3. Start dev server: npm run dev
# 4. The app uses a server-only API route at /api/gemini to call Gemini. The key must be present in the environment for that route to work.
